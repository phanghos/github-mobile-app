import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    borderRadius: 24,
    marginRight: 4,
    paddingHorizontal: 6,
    paddingVertical: 2,
  },
  text: { fontWeight: '600' },
});
